language: python

sudo: false

addons:
  apt:
    packages:
    - libffi-dev
    - libsasl2-dev
    - libldap2-dev

env:
  - TOX_ENV=py27

install:
  - pip install -U tox

script:
  - tox -e $TOX_ENV

